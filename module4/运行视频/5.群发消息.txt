1.启动服务器有客户端连接后通过ConnectInfoMgr管理类来添加新建的连接，并新建一个线程去接受客户端发送的消息
2.新建一个消息队列，有客户端发送消息后将消息添加到队列中
3.服务器有一个专门的线程去获取消息队列中的消息，为空时阻塞，不为空时转发给剩余的用户
4.客户端有一个发送消息和接收消息的线程，发送消息的线程会提示用户输入发送的消息并发送到服务器，接收消息的线程会接收服务器发送的消息并展示。